<p id="notice"><%= notice %></p>

<h1>Project一覧</h1>

<table>
  <thead>
    <tr>
      <th>名前</th>
      <th>内容</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.where(done_flag: false).each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= project.description %></td>
        <td><%= link_to "Show", project %></td>
        <td><%= link_to "Edit", edit_project_path(project) %></td>
        <td><%= link_to "Destroy", project, method: :delete, data: { confirm: "Are you sure?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "Projectを追加", new_project_path %>
